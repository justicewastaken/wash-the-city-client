# Memory — March 13, 2026

## Identity
- Human: Justice (username: justiceirl, Telegram: 5606165796)
- Assistant: Lisa
- Workspace: /root/.openclaw/workspace

## Setup & Configuration
- Enabled shell execution by changing OpenClaw config: `tools.profile` from "messaging" to "default" and restarted gateway.
- Installed skills: openai-whisper, himalaya, summarize, blogwatcher, github, gog, clouthub, coding-agent (ready), tmux (ready)
- Set up transcription with faster-whisper: created Python wrapper script for fast local transcription.

## Integrations
### Google Workspace (Gmail, Calendar, Drive, etc.)
- Authenticated via OAuth using service account (client_secret.json saved)
- Account: justiceforanything@gmail.com
- Skills: gog installed and configured; `GOG_KEYRING_PASSWORD` set to "opensesame"

### Email
- Gmail access working; used to check unread and mark many as read.
- Note: himalaya config not needed since gog covers email.

### Calendar
- Discovered multiple calendars (primary, JusticeWorkCalendar, etc.)
- Cleaned up recurring personal events (removed workouts, meals)
- Added recurring event: "Eau Claire Sips & Strategy" (biweekly marketing meetup, Wed 3:30-5pm CT) starting 2026-03-18 in primary calendar.
- Morning briefing scheduled via cron: daily 7am CT. Script: `/root/.openclaw/workspace/morning_briefing.py`. Sends Telegram message with weather (Eau Claire WI), today's events from all calendars, placeholder goals.

### Transcription
- faster-whisper installed in venv at `~/.venvs/whisper`. Script: `transcribe_fast.py` works quickly.

## MrTok TikTok Slideshow Project (in progress)
Goal: Automate generation and posting of TikTok slideshows for PupJewelry.com (pet jewelry).

- Installed `larry` skill (reference implementation) from ClawHub.
- Created workspace: `/root/.openclaw/workspace/mr-tok-pipeline`
  - scene_library/: 3 JSON scene definitions (POV dog walk, product flat lay, etc.)
  - personas/: dog_mom.json
  - src/: scripts for image generation, overlay, pipeline
  - output/: test images, final slide prototype
- Image generation:
  - Pollinations.ai unstable; Vertex AI (Imagen/Gemini) credentials obtained (service account saved to `gemini_service_account.json`) but integration not yet working.
  - Fallback: Using placeholder solid-color backgrounds + text overlay via ImageMagick.
- Overlay: ImageMagick script `overlay_imagemagick.sh` creates TikTok-style text (white with black stroke) using Montserrat font.
- Created test slide: `output/final_imagemagick.png` (beige bg, top and bottom text).
- Postiz: MCP endpoint noted (`https://mcp.postiz.com/mcp/018383e819bc8fa843d5b546904ad5db68deffc8a3c05a432d8bc160743829cb`). Account created; API integration pending.
- Reference screenshots: 5 uploaded for analysis; vision-based scene extraction attempted but not yet functional.

### Next steps for MrTok
1. Get Gemini/Vertex AI image generation working (fix API calls).
2. Analyze reference images to refine scene library and persona.
3. Generate 6-slide carousel with Gemini images and text overlays.
4. Connect to Postiz via MCP to auto-draft posts to TikTok.
5. Optionally build OpenClaw skill to manage the pipeline end-to-end.

## Other Notes
- Upwork proposal drafted for OpenClaw automation gig ($1k, dynamic PDF business). Not yet sent.
- Sub-agent (coding-agent) not yet spawnable via slash; using direct exec works.
- Browser tool not functional due to missing browser binary.

## Open Questions
- Refine TikTok slideshow style based on reference images.
- Confirm final CTA and product details (currently PupJewelry.com • Link in bio).
- Decide on image generation model (Gemini 2.5 Pro vs other) and finalize integration.
